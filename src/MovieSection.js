import API from './API';

const MovieSectionF = () => {

    return (
        <section>
            <div className="MovieSection">
                <API/>
            </div>
        </section>
    );
};

export default MovieSectionF;